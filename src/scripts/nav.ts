// const pagesLink = document.getElementById('pagesLink');
// const megaItem  = pagesLink?.closest('.mega');
// const canHover  = window.matchMedia('(hover: hover)').matches;

// if (megaItem && !canHover) {
//   pagesLink.addEventListener('click', e => {
//     e.preventDefault();
//     megaItem.classList.toggle('open');
//   });
//   document.addEventListener('click', e => {
//     if (!megaItem.contains(e.target)) megaItem.classList.remove('open');
//   });
// }
